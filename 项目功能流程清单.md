# 项目功能流程清单

这份文档用于熟悉当前项目代码。阅读顺序建议从前端路由开始，再沿着 API、后端 Controller、Service/Repository、数据库表往下看。

## 1. 页面与路由

| 功能页 | 路由 | 前端文件 |
|---|---|---|
| 登录/注册 | `/auth` | `frontend/src/modules/auth/pages/AuthPage.vue` |
| 航班查询、同步、AI 建议 | `/flights` | `frontend/src/modules/user-flights/pages/FlightSearchPage.vue` |
| 我的收藏 | `/favorites` | `frontend/src/modules/user-profile/pages/FavoritesPage.vue` |
| 搜索历史 | `/history` | `frontend/src/modules/user-profile/pages/SearchHistoryPage.vue` |
| 采集任务管理 | `/admin/crawl-jobs` | `frontend/src/modules/admin-crawl/pages/CrawlJobsPage.vue` |
| 数据源状态 | `/admin/data-sources` | `frontend/src/modules/admin-crawl/pages/DataSourceStatusPage.vue` |
| 退出登录 | 页面右上角用户菜单 | `frontend/src/shared/components/UserMenu.vue` |

路由配置在 `frontend/src/router/index.js`。除 `/auth` 外，其余页面都需要登录。登录态检查由 `frontend/src/auth/session.js` 的 `ensureAuthenticatedSession()` 完成。

## 2. 通用请求入口

前端所有 axios 请求都经过 `frontend/src/api/http.js`：

- 默认后端地址：`http://localhost:8080`
- 每次请求前从 `localStorage.token` 读取 token
- 如果存在 token，则加请求头 `Authorization: Bearer <token>`
- 如果后端返回 401，则清空本地会话并触发退出状态

后端通过自定义登录拦截器解析 token，并把当前用户放到请求属性 `user` 中。Controller 中常见的 `@RequestAttribute("user") User user` 就来自这里。

认证模块当前已经按职责拆分：

```text
backend/src/main/java/com/example/flight/auth/
  controller/    认证接口入口
  service/       登录、注册、默认用户初始化
  repository/    app_user 和 user_token 表访问
  dto/           登录、注册请求对象
  model/         User、UserToken 领域对象
  web/           LoginInterceptor 登录拦截器
```

航班模块当前也按职责拆分：

```text
backend/src/main/java/com/example/flight/flight/
  controller/        航班查询、详情、价格历史接口
  repository/        flight 和 flight_price_snapshot 表访问，以及查询端口
  dto/               航班查询条件
  model/             Flight、FlightPriceSnapshot 响应模型
  service/           启动后的历史样例数据清理
  favorite/
    controller/      收藏接口
    repository/      favorite 表访问
    dto/             创建收藏请求
    model/           FavoriteRecord 收藏响应模型
  history/
    controller/      搜索历史接口
    service/         搜索历史记录业务编排
    repository/      search_history 表访问
    dto/             记录搜索历史的命令对象
    model/           SearchHistoryRecord 搜索历史响应模型
```

采集模块当前按职责拆分：

```text
backend/src/main/java/com/example/flight/crawl/
  controller/        普通采集触发和最新任务查询
  service/           启动爬虫进程、指定日期同步编排
  repository/        crawl_job 表访问
  dto/               采集请求参数
  model/             CrawlJob、FlightSyncResult
  port/              AI 模块调用采集同步能力的端口
  admin/
    controller/      管理端采集任务、同步、数据源状态接口
    service/         数据源配置状态判断
    dto/             管理端创建采集任务请求
```

## 3. 登录、注册、会话

| 动作 | 前端方法 | 前端 API | 后端接口 | 后端代码 | 数据表 |
|---|---|---|---|---|---|
| 注册 | `AuthPage.submit()` | `register()` | `POST /api/auth/register` | `auth/controller/AuthController -> auth/service/AuthService` | `app_user`, `user_token` |
| 登录 | `AuthPage.submit()` | `login()` | `POST /api/auth/login` | `auth/controller/AuthController -> auth/service/AuthService` | `app_user`, `user_token` |
| 会话检查 | `ensureAuthenticatedSession()` | `fetch /api/auth/me` | `GET /api/auth/me` | `auth/controller/AuthController.me()` | `user_token`, `app_user` |
| 退出 | `UserMenu.handleLogout()` | `logout()` | `POST /api/auth/logout` | `auth/controller/AuthController.logout()` | `user_token` |

登录或注册成功后，前端保存：

- `localStorage.token`
- `localStorage.user`

退出时会删除 token、用户信息和航班搜索页缓存。

## 4. 航班查询

| 层级 | 位置 |
|---|---|
| 前端页面 | `FlightSearchPage.vue` |
| 页面方法 | `submitSearch()`、`buildQueryParams()` |
| 前端 API | `fetchFlights(params)` in `frontend/src/api/flightApi.js` |
| 后端接口 | `GET /api/flights` |
| 后端代码 | `flight/controller/FlightController.search()` |
| 数据访问 | `flight/repository/FlightRepository.search(criteria, user.id())` |
| 数据表 | `flight`，同时可能写 `search_history` |

流程：

1. 用户选择出发地、到达地、日期、数据源等筛选条件。
2. `submitSearch()` 调用 `fetchFlights(buildQueryParams())`。
3. 后端 `flight/controller/FlightController.search()` 接收查询参数。
4. `flight/repository/FlightRepository` 按条件查询 `flight`。
5. 如果本次查询带筛选条件，后端调用 `flight/history/service/SearchHistoryService.record()` 写入 `search_history`。
6. 前端更新航班列表，并默认选中当前页第一条航班。

## 5. 航班详情与价格历史

| 动作 | 前端方法 | 前端 API | 后端接口 | 数据表 |
|---|---|---|---|---|
| 查看详情 | `selectFlight(flight)` | `fetchFlight(id)` | `GET /api/flights/{id}` | `flight` |
| 查看价格历史 | `selectFlight(flight)` | `fetchPriceHistory(id)` | `GET /api/flights/{id}/price-history` | `flight_price_snapshot` |

流程：

1. 用户点击航班表格中的某一行。
2. `selectFlight()` 并发请求详情和价格历史。
3. 详情展示在 `FlightDetailCard.vue`。
4. 价格历史展示在 `PriceHistoryChart.vue`。

## 6. 指定日期航班同步

| 层级 | 位置 |
|---|---|
| 前端页面 | `FlightSearchPage.vue` |
| 页面方法 | `submitSync()`、`syncToday()`、`applySuccessfulSync()` |
| 前端 API | `syncFlights({ airportCode, date })` |
| 后端接口 | `POST /api/admin/flights/sync?airportCode=XXX&date=YYYY-MM-DD` |
| 后端入口 | `crawl/admin/controller/AdminFlightSyncController.sync()` |
| 后端服务 | `crawl/service/FlightSyncService.runAirportSyncJob()`、`crawl/service/CrawlService.runCrawler()` |
| 爬虫 | `crawler/flight_crawler/spiders/aerodatabox_flights.py` |
| 写库 | `crawler/flight_crawler/pipelines.py` |
| 数据表 | `crawl_job`, `flight`, `flight_price_snapshot` |

流程：

1. 前端传机场三字码和日期。
2. 后端校验 `airportCode`、`date` 和 AeroDataBox 数据源配置。
3. 后端通过 `crawl/service/CrawlService` 启动 Docker Compose 爬虫命令。
4. Scrapy 爬虫请求 AeroDataBox。
5. `MysqlPipeline.open_spider()` 插入一条 `crawl_job`，状态为 `RUNNING`。
6. `MysqlPipeline.process_item()` 写入或更新 `flight`，并插入 `flight_price_snapshot`。
7. `MysqlPipeline.close_spider()` 更新 `crawl_job` 的最终状态、成功数和失败数。
8. 后端返回最新的 `crawl_job` 给前端。
9. 前端调用 `applySuccessfulSync()`，把数据源切到 `aerodatabox`，按同步日期重新查询航班。

关键点：后端不直接写航班数据。真正写 `flight` 和 `flight_price_snapshot` 的是爬虫 pipeline。

## 7. 收藏与取消收藏

| 动作 | 前端组件/页面 | 前端方法 | 前端 API | 后端接口 | 数据表 |
|---|---|---|---|---|---|
| 收藏 | `FavoriteButton.vue` | `toggle()` | `addFavorite({ flightId })` | `POST /api/me/favorites` | `favorite` |
| 取消收藏 | `FavoriteButton.vue` | `toggle()` | `removeFavorite(favoriteId)` | `DELETE /api/me/favorites/{favoriteId}` | `favorite` |
| 收藏列表 | `FavoritesPage.vue` | `loadFavorites()` | `fetchFavorites()` | `GET /api/me/favorites` | `favorite join flight` |

后端代码：

- `flight/favorite/controller/FavoriteController`
- `flight/favorite/repository/FavoriteRepository`

流程：

1. 航班表格或详情卡中的星标按钮触发 `FavoriteButton.toggle()`。
2. 添加收藏时，后端检查航班是否存在，并保证同一用户同一航班只收藏一次。
3. 删除收藏时，后端按当前用户和 `favoriteId` 删除，避免删到别人的收藏。
4. 收藏页通过 `favorite join flight` 展示收藏航班的完整信息。

## 8. 搜索历史

| 层级 | 位置 |
|---|---|
| 前端页面 | `SearchHistoryPage.vue` |
| 页面方法 | `loadHistory()` |
| 前端 API | `fetchSearchHistory()` |
| 后端接口 | `GET /api/me/search-history` |
| 后端代码 | `flight/history/controller/SearchHistoryController -> flight/history/service/SearchHistoryService -> flight/history/repository/SearchHistoryRepository` |
| 数据表 | `search_history` |

搜索历史不是前端主动写的。它在 `GET /api/flights` 查询时由后端自动记录。

`search_history` 表由 `DatabaseInitializer.java` 补建；如果只看 `infra/mysql/init.sql`，可能会漏掉这个表。

## 9. 采集任务管理

| 动作 | 前端页面 | 前端方法 | 前端 API | 后端接口 | 数据表 |
|---|---|---|---|---|---|
| 查看数据源状态 | `CrawlJobsPage.vue` | `loadStatuses()` | `listDataSourceStatuses()` | `GET /api/admin/data-sources/status` | 不直接读表 |
| 查看任务列表 | `CrawlJobsPage.vue` | `loadJobs()` | `listCrawlJobs()` | `GET /api/admin/crawl-jobs` | `crawl_job` |
| 创建采集任务 | `CrawlJobsPage.vue` | `submitJob()` | `createCrawlJob(payload)` | `POST /api/admin/crawl-jobs` | `crawl_job`, `flight`, `flight_price_snapshot` |

后端代码：

- `crawl/admin/controller/AdminCrawlController`
- `crawl/admin/service/DataSourceStatusService`
- `crawl/service/CrawlService`
- `crawl/repository/CrawlRepository`

采集任务管理页和航班查询页的“指定日期同步”最终都会进入 `crawl/service/CrawlService.runCrawler()`，差别只是前端入口和请求参数不同。

## 10. 数据源状态

| 层级 | 位置 |
|---|---|
| 前端页面 | `DataSourceStatusPage.vue` |
| 页面方法 | `loadStatuses()` |
| 前端 API | `listDataSourceStatuses()` |
| 后端接口 | `GET /api/admin/data-sources/status` |
| 后端代码 | `crawl/admin/controller/DataSourceStatusController -> crawl/admin/service/DataSourceStatusService` |

判断逻辑：

- `app.crawler.command` 不为空
- `AERODATABOX_KEY` 或 `aerodatabox.key` 有值

满足时 `aerodatabox` 显示为 configured。

## 11. AI 建议与保留对话接口

| 功能 | 前端入口 | 前端 API | 后端接口 | 数据表 |
|---|---|---|---|---|
| AI 出行建议 | `FlightSearchPage.vue` | `requestAdvice(message)` | `POST /api/ai/advice` | 可能读取 `price_context` |
| AI 对话 | 当前无独立页面 | `client.js` 兼容方法 | `/api/ai/conversations/**` | `conversation_session`, `conversation_message` |

AI 不是当前主业务核心。当前主线仍是航班采集、查询、收藏、历史和采集任务管理。

## 12. 数据表速查

| 表 | 用途 |
|---|---|
| `flight` | 航班主数据 |
| `flight_price_snapshot` | 航班价格历史快照 |
| `crawl_job` | 每次爬虫采集任务记录 |
| `flight_validation_failure` | 航班数据校验失败记录 |
| `app_user` | 用户账号 |
| `user_token` | 登录 token |
| `favorite` | 用户收藏 |
| `search_history` | 用户搜索历史，由 `DatabaseInitializer` 创建 |
| `price_context` | AI 建议相关上下文 |
| `conversation_session` | AI 对话会话 |
| `conversation_message` | AI 对话消息 |

## 13. 推荐源码阅读顺序

1. `frontend/src/router/index.js`
2. `frontend/src/api/http.js`
3. `backend/src/main/java/com/example/flight/auth/web/LoginInterceptor.java`
4. `backend/src/main/java/com/example/flight/auth/controller/AuthController.java`
5. `frontend/src/modules/user-flights/pages/FlightSearchPage.vue`
6. `frontend/src/api/flightApi.js`
7. `backend/src/main/java/com/example/flight/flight/controller/FlightController.java`
8. `backend/src/main/java/com/example/flight/flight/repository/FlightRepository.java`
9. `backend/src/main/java/com/example/flight/crawl/service/CrawlService.java`
10. `crawler/flight_crawler/pipelines.py`
11. `infra/mysql/init.sql`
12. `backend/src/main/java/com/example/flight/config/DatabaseInitializer.java`
