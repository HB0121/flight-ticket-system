# Task 9 Report: Add favorite-related i18n keys

## Status
Success

## What changed
- **zh-CN.js**: Added `favorite` and `unfavorite` keys to `common.actions`; added a `flights.favorite` sub-block with `added`, `removed`, and `failed` keys.
- **en-US.js**: Added `favorite` and `unfavorite` keys to `common.actions`; added a `flights.favorite` sub-block with `added`, `removed`, and `failed` keys.

## Build result
Vite build completed successfully (`✓ built in 7.42s`).

## Concerns
None.
